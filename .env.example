# === Production (Railway) ===
# Add PostgreSQL plugin in Railway → DATABASE_URL is set automatically

# Your app URL (Railway gives this after deploy, e.g. https://ooobed.up.railway.app)
NEXT_PUBLIC_BASE_URL="https://your-app.up.railway.app"

# From @BotFather
TELEGRAM_BOT_TOKEN=""
NEXT_PUBLIC_BOT_USERNAME="your_bot_username"

# Session secret (generate: openssl rand -base64 32)
SESSION_SECRET="your-secret-key-at-least-32-characters-long"

# === Local dev with PostgreSQL ===
# DATABASE_URL="postgresql://user:password@localhost:5432/ooobed"
# NEXT_PUBLIC_BASE_URL="http://localhost:3000"
